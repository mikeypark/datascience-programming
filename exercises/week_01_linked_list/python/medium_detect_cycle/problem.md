# Week 01 / Python (중간) — 사이클 찾기 (플로이드 알고리즘)

연결 리스트의 마지막 노드가 앞쪽 노드를 가리키면 고리가 생깁니다.
이때 사이클이 **시작되는 노드**와 사이클의 **길이**를 찾는 문제입니다.

```
1 -> 2 -> 3 -> 4
          ^         |
          +---------+

detect_cycle(head)  ->  값이 3인 노드
cycle_length(head)  ->  2   (3 -> 4 -> 3)
```

## 구현할 함수

```python
def detect_cycle(head: Node | None) -> Node | None: ...  # 사이클 시작 노드, 없으면 None
def cycle_length(head: Node | None) -> int: ...          # 사이클 길이, 없으면 0
```

## 조건

- **추가 메모리는 상수만 사용해 주세요.**
  방문한 노드를 `set` 이나 `dict` 에 모으는 풀이는 답으로 인정하지 않습니다.
  fast and slow pointer (토끼와 거북이) 로 풀어 주세요.
- 반환하는 것은 값이 아니라 **노드 객체 그 자체**입니다. 채점기가 `is` 로 확인합니다.
- 사이클이 리스트 전체를 감쌀 수도 있고(머리가 곧 시작점), 노드 하나가 자기 자신을 가리킬 수도 있습니다.

## 채점

```sh
uv run runner.py test 1 --task detect_cycle
```

## 생각해 볼 것

- slow 가 한 칸, fast 가 두 칸씩 갈 때 둘은 왜 반드시 만나게 될까요?
- 만난 지점에서 한 포인터를 머리로 되돌리고 둘 다 한 칸씩 옮기면 왜 시작점에서 만날까요?
  종이에 `머리에서 시작점까지 = a`, `시작점에서 만난 곳까지 = b`, `사이클 길이 = L` 로 두고
  식을 세워 보시면 금방 보입니다.
- `cycle_length` 는 `detect_cycle` 을 다시 부르지 않고도 구할 수 있을까요?
