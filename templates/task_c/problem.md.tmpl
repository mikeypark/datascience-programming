# Week {{week}} / C ({{difficulty_label}}) — (제목)

(무엇을 구현하는 문제인지 한두 문단으로 적어 주세요)

## 시그니처

```c
int solve(const int *values, int n, int *out, int out_capacity);
```

- 반환값은 `out` 에 쓴 개수입니다. 오류이거나 용량이 부족하면 `-1` 을 반환합니다.

## 조건

- (제약 조건 1)
- 할당한 메모리는 반환하기 전에 모두 해제해 주세요.

## 채점

```sh
uv run runner.py test {{week_num}} --task {{task}}
```

테스트가 `solution.c` 를 공유 라이브러리로 빌드한 다음 ctypes 로 직접 호출합니다.

## 생각해 볼 것

- (스스로 답해 볼 만한 질문)
